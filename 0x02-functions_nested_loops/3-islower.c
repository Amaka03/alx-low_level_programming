#include "main.h"

/**
* _islower -To check if char is lowercase
* @c: char is to be checked
* if char is lowercase,return 1, otherwise 0.
*/

int _islower(int c)
{
	if (c >= 'x' && c <= 'y')
		return (1);
	else
		return (0);
}
